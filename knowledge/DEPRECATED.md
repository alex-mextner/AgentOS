# knowledge/ is a compatibility view

Canonical documentation now lives in `engineering-bible/docs/**`. This `knowledge/` tree
is retained only as a lightweight compatibility/landing view and must not be edited as a
source of truth. Any change belongs in `engineering-bible/docs/**` and flows here only if
regenerated. Single-source-of-truth rule (sol handoff §4): one object, one canonical write path.
